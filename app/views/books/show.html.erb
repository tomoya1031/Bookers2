<div class="container">
	<div class="row">
		<div class="col-xs-3">
		    <%= render 'books/sidebar', book: @new_book %>
<!-- 			<div>
				<h2>User info</h2>
			</div>
			<div>
				<%= attachment_image_tag @book.user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %>
			</div>
			<table>
				<tbody>
					<tr>
						<th>name</th>
						<th><%= @book.user.name %></th>
					</tr>
					<tr>
						<th>introduction</th>
						<th><%= @book.user.introduction %></th>
					</tr>
				</tbody>
			</table>
			<div classa="btn btn-info">
					<%= link_to edit_user_path(current_user.id) , class: "btn btn-success" do %>
					<span class="glyphicon glyphicon-wrench" aria-hidden="true"></span>
					<% end %>
			</div>
			<h2>New book</h2>
			<%= form_for(@new_book, url: {controller: 'books', action: 'create'}) do |f| %>
			    <div class="box field">
			    	<div>
			    		<%= f.label :title, "Titele" %>
			    	</div>
			    	<div>
			    		<%= f.text_field :title %>
			    	</div>
			    </div>
			    <div class="box">
			    	<div>
			    		<%= f.label :title, "Opinion" %>
			    	</div>
			    	<div>
			    		<%= f.text_area :body %>
			    	</div>
			    </div>
			    <%= f.submit 'Create Book' ,class:"submit" %>
			<% end %>
		</div> -->
		<div class="col-xs-9">

<h1>Book detail</h1>
<table>
	<tbody>
		<tr>
			<td>
				<div>
				<%= attachment_image_tag @book.user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %>
				<%= link_to user_path(@book.user.id) do %>
					<%= @book.user.name %>
				<% end %>
				<div>
			</td>
			<td><%= @book.title %></td>
			<td><%= @book.body %></td>
			<td>
			    <% if @book.user == current_user %>
			        <%= link_to 'Edit', edit_book_path(@book), class: "btn btn-info" %>
			        <%= link_to 'Destroy', book_path(@book), method: :delete, data: {confirm:"Are you sure?"} ,class: "btn btn-info" %>
			    <% end %>
			</td>
		</tr>
	</tbody>
</table>
</div>
</div>

